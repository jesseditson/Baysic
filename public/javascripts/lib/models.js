var generateModel = function(model){
  // TODO: generate knockout bindings and add rest methods
};

exports = {
  Post : generateModel(require('../../models/Post')),
  User : generateModel(require('../../models/User'))
}
